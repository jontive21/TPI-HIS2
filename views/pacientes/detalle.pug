a.btn.btn-info.mt-2(href=`/pacientes/${paciente.id}/evaluacion`) Evaluación de Enfermería

//- Formulario de evaluación de enfermería
h4 Evaluación de Enfermería
form(method="POST" action=`/pacientes/${paciente.id}/evaluacion`)
  .mb-3
    label.form-label Alergias
    textarea.form-control(name="alergias")= paciente.alergias || ''
  
  .row.mb-3
    .col
      label.form-label Presión Arterial
      input.form-control(
        type="text"
        name="presion_arterial"
        value=paciente.presion_arterial || ''
        pattern="\\d{1,3}/\\d{1,3}"
        title="Formato: 120/80"
      )
    .col
      label.form-label Frec. Cardíaca
      input.form-control(
        type="number"
        name="frecuencia_cardiaca"
        value=paciente.frecuencia_cardiaca || ''
        min="30"
        max="200"
      )
  
  button.btn.btn-primary(type="submit") Guardar Evaluación

if success_msg
  .alert.alert-success= success_msg
if error_msg
  .alert.alert-danger= error_msg