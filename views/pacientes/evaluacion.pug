form(method="POST" action=`/pacientes/${paciente.id}/evaluacion`)
  h4 Evaluación de Enfermería
  .form-row
    .form-group.col-md-6
      label(for="alergias") Alergias
      textarea.form-control(name="alergias")= paciente.alergias || ''
    .form-group.col-md-6
      label(for="medicamentos_actuales") Medicamentos Actuales
      textarea.form-control(name="medicamentos_actuales")= paciente.medicamentos_actuales || ''
  .form-row
    .form-group.col-md-4
      label(for="presion_arterial") Presión Arterial
      input.form-control(type="text" name="presion_arterial" value=paciente.presion_arterial || '')
    .form-group.col-md-4
      label(for="frecuencia_cardiaca") Frec. Cardíaca
      input.form-control(type="number" name="frecuencia_cardiaca" value=paciente.frecuencia_cardiaca || '')
    .form-group.col-md-4
      label(for="temperatura") Temperatura (°C)
      input.form-control(type="number" step="0.1" name="temperatura" value=paciente.temperatura || '')
  button.btn.btn-primary(type="submit") Guardar Evaluación