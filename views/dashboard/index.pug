extends ../layout

block content
  h1 #{title} // O tu título preferido
  p Texto de ejemplo.

  // INDENTA TODO LO SIGUIENTE PARA QUE ESTÉ DENTRO DE BLOCK CONTENT
  small Rol: #{user.rol} | Último acceso: #{lastAccess}

  .row.mt-4 // Estructura opcional con Bootstrap
    .col-md-3
      .card.text-center.mb-3
        .card-header Camas Disponibles
        .card-body
          .display-4#camas-disponibles #{camasDisponibles}
          small.text-muted de #{camasTotal} total
    // ... (y así para los otros indicadores, todos indentados)

  // ...resto de tu plantilla...